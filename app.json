{
    "scripts": {
      "dokku": {
        "predeploy": "python manage.py compilescss && python manage.py collectstatic --noinput -i docs -i *.scss",
        "postdeploy": "python manage.py migrate"
      }
    }
  }
